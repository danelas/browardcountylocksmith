{
  "name": "browardcounty-locksmith-blog-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Twice-daily AI blog post generator for Broward County Locksmith. Runs on GitHub Actions cron — drafts one SEO-optimized article per run via Claude, generates a branded hero image, and writes it into the root of this repo (the static site deployed at browardcountylocksmith.com).",
  "scripts": {
    "post": "tsx src/index.ts",
    "post:dry": "tsx src/index.ts --dry-run",
    "post:next": "tsx src/index.ts --print-next",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "dotenv": "^16.4.5",
    "openai": "^4.70.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
